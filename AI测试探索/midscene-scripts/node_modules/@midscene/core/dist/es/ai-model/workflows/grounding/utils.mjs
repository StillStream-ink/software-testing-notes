function formatLocateModelContext(modelRuntime) {
    const { modelFamily, modelName } = modelRuntime.config;
    return `modelName=${modelName ?? 'unset'} modelFamily=${modelFamily ?? 'unset'}`;
}
function hasLocateResult(input, resultKey) {
    if (!input || 'object' != typeof input) return false;
    const record = input;
    const locateResult = record[resultKey];
    return Array.isArray(locateResult) ? locateResult.length > 0 : void 0 !== locateResult;
}
export { formatLocateModelContext, hasLocateResult };

//# sourceMappingURL=utils.mjs.map