"use strict";
var __webpack_require__ = {};
(()=>{
    __webpack_require__.d = (exports1, definition)=>{
        for(var key in definition)if (__webpack_require__.o(definition, key) && !__webpack_require__.o(exports1, key)) Object.defineProperty(exports1, key, {
            enumerable: true,
            get: definition[key]
        });
    };
})();
(()=>{
    __webpack_require__.o = (obj, prop)=>Object.prototype.hasOwnProperty.call(obj, prop);
})();
(()=>{
    __webpack_require__.r = (exports1)=>{
        if ('undefined' != typeof Symbol && Symbol.toStringTag) Object.defineProperty(exports1, Symbol.toStringTag, {
            value: 'Module'
        });
        Object.defineProperty(exports1, '__esModule', {
            value: true
        });
    };
})();
var __webpack_exports__ = {};
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
    normalizeFileChooserAccept: ()=>normalizeFileChooserAccept,
    FileChooserAccepter: ()=>FileChooserAccepter,
    normalizeFileChooserAcceptInAllowedDir: ()=>normalizeFileChooserAcceptInAllowedDir,
    withFileChooser: ()=>withFileChooser
});
const external_node_path_namespaceObject = require("node:path");
const external_utils_js_namespaceObject = require("./utils.js");
function _define_property(obj, key, value) {
    if (key in obj) Object.defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
function normalizeFileChooserAccept(files) {
    const filesArray = Array.isArray(files) ? files : [
        files
    ];
    return (0, external_utils_js_namespaceObject.normalizeFilePaths)(filesArray);
}
function normalizeFileChooserAcceptInAllowedDir(files, allowedDir) {
    const absoluteAllowedDir = (0, external_node_path_namespaceObject.resolve)(allowedDir);
    const filesArray = Array.isArray(files) ? files : [
        files
    ];
    const resolvedFiles = filesArray.map((file)=>{
        const resolvedFile = (0, external_node_path_namespaceObject.resolve)(absoluteAllowedDir, file);
        const relativePath = (0, external_node_path_namespaceObject.relative)(absoluteAllowedDir, resolvedFile);
        if ('..' === relativePath || relativePath.startsWith(`..${external_node_path_namespaceObject.sep}`) || (0, external_node_path_namespaceObject.isAbsolute)(relativePath)) throw new Error(`File chooser path must be inside the configured fileChooserAllowedDir: ${file}`);
        return resolvedFile;
    });
    return (0, external_utils_js_namespaceObject.normalizeFilePaths)(resolvedFiles);
}
class FileChooserAccepter {
    async register(files) {
        await this.replaceAcceptedFiles(normalizeFileChooserAccept(files));
    }
    async registerFromAllowedDir(files, allowedDir) {
        await this.replaceAcceptedFiles(normalizeFileChooserAcceptInAllowedDir(files, allowedDir));
    }
    async replaceAcceptedFiles(acceptedFiles) {
        const previousRegistrationError = await this.clear();
        if (previousRegistrationError) throw previousRegistrationError;
        if (!acceptedFiles.length) return;
        if (!this.interfaceInstance.registerFileChooserListener) throw new Error(`File upload is not supported on ${this.interfaceInstance.interfaceType}`);
        this.registration = await this.interfaceInstance.registerFileChooserListener(async (chooser)=>{
            await chooser.accept(acceptedFiles);
        });
    }
    async clear() {
        const registration = this.registration;
        this.registration = void 0;
        if (!registration) return;
        try {
            return await registration.getError();
        } finally{
            registration.dispose();
        }
    }
    constructor(interfaceInstance){
        _define_property(this, "interfaceInstance", void 0);
        _define_property(this, "registration", void 0);
        this.interfaceInstance = interfaceInstance;
    }
}
async function withFileChooser(interfaceInstance, fileChooserAccept, action) {
    if (!fileChooserAccept?.length) return action();
    if (!interfaceInstance.registerFileChooserListener) throw new Error(`File upload is not supported on ${interfaceInstance.interfaceType}`);
    const handler = async (chooser)=>{
        await chooser.accept(fileChooserAccept);
    };
    const { dispose, getError } = await interfaceInstance.registerFileChooserListener(handler);
    try {
        const result = await action();
        const error = await getError();
        if (error) throw error;
        return result;
    } finally{
        dispose();
    }
}
exports.FileChooserAccepter = __webpack_exports__.FileChooserAccepter;
exports.normalizeFileChooserAccept = __webpack_exports__.normalizeFileChooserAccept;
exports.normalizeFileChooserAcceptInAllowedDir = __webpack_exports__.normalizeFileChooserAcceptInAllowedDir;
exports.withFileChooser = __webpack_exports__.withFileChooser;
for(var __rspack_i in __webpack_exports__)if (-1 === [
    "FileChooserAccepter",
    "normalizeFileChooserAccept",
    "normalizeFileChooserAcceptInAllowedDir",
    "withFileChooser"
].indexOf(__rspack_i)) exports[__rspack_i] = __webpack_exports__[__rspack_i];
Object.defineProperty(exports, '__esModule', {
    value: true
});

//# sourceMappingURL=file-chooser.js.map