"use strict";
var __webpack_require__ = {};
(()=>{
    __webpack_require__.d = (exports1, definition)=>{
        for(var key in definition)if (__webpack_require__.o(definition, key) && !__webpack_require__.o(exports1, key)) Object.defineProperty(exports1, key, {
            enumerable: true,
            get: definition[key]
        });
    };
})();
(()=>{
    __webpack_require__.o = (obj, prop)=>Object.prototype.hasOwnProperty.call(obj, prop);
})();
(()=>{
    __webpack_require__.r = (exports1)=>{
        if ('undefined' != typeof Symbol && Symbol.toStringTag) Object.defineProperty(exports1, Symbol.toStringTag, {
            value: 'Module'
        });
        Object.defineProperty(exports1, '__esModule', {
            value: true
        });
    };
})();
var __webpack_exports__ = {};
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
    parseXMLPlanningResponse: ()=>parseXMLPlanningResponse,
    parseSubGoalsFromXML: ()=>parseSubGoalsFromXML,
    parseMarkFinishedIndexes: ()=>parseMarkFinishedIndexes
});
const xml_js_namespaceObject = require("../../shared/xml.js");
function parseSubGoalsFromXML(xmlContent) {
    const subGoals = [];
    const regex = /<sub-goal\s+index="(\d+)"\s+status="(pending|finished)"(?:\s*\/>|>([\s\S]*?)<\/sub-goal>)/gi;
    let match = regex.exec(xmlContent);
    while(null !== match){
        const index = Number.parseInt(match[1], 10);
        const status = match[2];
        const description = match[3]?.trim() || '';
        subGoals.push({
            index,
            status,
            description
        });
        match = regex.exec(xmlContent);
    }
    return subGoals;
}
function parseMarkFinishedIndexes(xmlContent) {
    const indexes = [];
    const regex = /<sub-goal\s+index="(\d+)"\s+status="finished"\s*\/>/gi;
    let match = regex.exec(xmlContent);
    while(null !== match){
        indexes.push(Number.parseInt(match[1], 10));
        match = regex.exec(xmlContent);
    }
    return indexes;
}
function parseXMLPlanningResponse(xmlString, jsonParser) {
    const thought = (0, xml_js_namespaceObject.extractXMLTag)(xmlString, 'planning');
    const memory = (0, xml_js_namespaceObject.extractXMLTag)(xmlString, 'memory');
    const log = (0, xml_js_namespaceObject.extractXMLTag)(xmlString, 'log') || '';
    const error = (0, xml_js_namespaceObject.extractXMLTag)(xmlString, 'error');
    const actionType = (0, xml_js_namespaceObject.extractXMLTag)(xmlString, 'action-type');
    const actionParamStr = (0, xml_js_namespaceObject.extractXMLTag)(xmlString, 'action-param-json');
    const completeGoalRegex = /<complete\s+success="(true|false)">([\s\S]*?)<\/complete>/i;
    const completeGoalMatch = xmlString.match(completeGoalRegex);
    let finalizeMessage;
    let finalizeSuccess;
    if (completeGoalMatch) {
        finalizeSuccess = 'true' === completeGoalMatch[1];
        finalizeMessage = completeGoalMatch[2]?.trim() || void 0;
    }
    const updatePlanContent = (0, xml_js_namespaceObject.extractXMLTag)(xmlString, 'update-plan-content');
    const markSubGoalDone = (0, xml_js_namespaceObject.extractXMLTag)(xmlString, 'mark-sub-goal-done');
    const updateSubGoals = updatePlanContent ? parseSubGoalsFromXML(updatePlanContent) : void 0;
    const markFinishedIndexes = markSubGoalDone ? parseMarkFinishedIndexes(markSubGoalDone) : void 0;
    let action = null;
    if (actionType && 'null' !== actionType.toLowerCase()) {
        const type = actionType.split('<')[0].trim();
        let param;
        if (actionParamStr) try {
            param = jsonParser(actionParamStr, {
                source: 'planning-action-param',
                preserveStringValueKeys: 'input' === type.toLowerCase() ? [
                    'value'
                ] : void 0
            });
        } catch (error) {
            throw new Error(`Failed to parse action-param-json: ${error}`);
        }
        action = {
            type,
            ...void 0 !== param ? {
                param
            } : {}
        };
    }
    return {
        ...thought ? {
            thought
        } : {},
        ...memory ? {
            memory
        } : {},
        log,
        ...error ? {
            error
        } : {},
        action,
        ...void 0 !== finalizeMessage ? {
            finalizeMessage
        } : {},
        ...void 0 !== finalizeSuccess ? {
            finalizeSuccess
        } : {},
        ...updateSubGoals?.length ? {
            updateSubGoals
        } : {},
        ...markFinishedIndexes?.length ? {
            markFinishedIndexes
        } : {}
    };
}
exports.parseMarkFinishedIndexes = __webpack_exports__.parseMarkFinishedIndexes;
exports.parseSubGoalsFromXML = __webpack_exports__.parseSubGoalsFromXML;
exports.parseXMLPlanningResponse = __webpack_exports__.parseXMLPlanningResponse;
for(var __rspack_i in __webpack_exports__)if (-1 === [
    "parseMarkFinishedIndexes",
    "parseSubGoalsFromXML",
    "parseXMLPlanningResponse"
].indexOf(__rspack_i)) exports[__rspack_i] = __webpack_exports__[__rspack_i];
Object.defineProperty(exports, '__esModule', {
    value: true
});

//# sourceMappingURL=standard-planning-parser.js.map