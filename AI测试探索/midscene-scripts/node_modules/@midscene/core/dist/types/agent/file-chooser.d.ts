import type { AbstractInterface } from '../device';
export type FileChooserAccept = string | string[];
export declare function normalizeFileChooserAccept(files: FileChooserAccept): string[];
export declare function normalizeFileChooserAcceptInAllowedDir(files: FileChooserAccept, allowedDir: string): string[];
/**
 * Holds the current file chooser configuration for one aiAct execution.
 * Registering new files replaces the previous configuration; callers must
 * clear the accepter when the aiAct scope ends.
 */
export declare class FileChooserAccepter {
    private readonly interfaceInstance;
    private registration?;
    constructor(interfaceInstance: AbstractInterface);
    register(files: FileChooserAccept): Promise<void>;
    registerFromAllowedDir(files: FileChooserAccept, allowedDir: string): Promise<void>;
    private replaceAcceptedFiles;
    clear(): Promise<Error | undefined>;
}
export declare function withFileChooser<T>(interfaceInstance: AbstractInterface, fileChooserAccept: string[] | undefined, action: () => Promise<T>): Promise<T>;
