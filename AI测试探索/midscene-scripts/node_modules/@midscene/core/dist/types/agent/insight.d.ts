import type { TUserPrompt } from '../ai-model/index';
import type { ModelRuntime } from '../ai-model/models';
import type { AgentAssertResult, AssertOptions, InsightAPI, QueryOptions, ServiceExtractParam, UIContext } from '../types';
import { type TaskExecutor } from './tasks';
type InsightTaskExecutor = Pick<TaskExecutor, 'createTypeQueryExecution'>;
/** Execute read-only AI operations against either live or fixed UI context. */
export declare class Insight implements InsightAPI {
    private readonly taskExecutor;
    private readonly resolveModelRuntime;
    private readonly getUIContext?;
    constructor(taskExecutor: InsightTaskExecutor, resolveModelRuntime: () => ModelRuntime, getUIContext?: (() => UIContext) | undefined);
    private executionOptions;
    aiQuery<ReturnType = any>(demand: ServiceExtractParam, options?: QueryOptions): Promise<ReturnType>;
    aiBoolean(prompt: TUserPrompt, options?: QueryOptions): Promise<boolean>;
    aiNumber(prompt: TUserPrompt, options?: QueryOptions): Promise<number>;
    aiString(prompt: TUserPrompt, options?: QueryOptions): Promise<string>;
    aiAsk(prompt: TUserPrompt, options?: QueryOptions): Promise<string>;
    private queryPrimitive;
    aiAssert(assertion: TUserPrompt, message?: string, options?: AssertOptions): Promise<AgentAssertResult | undefined>;
}
export {};
