import { WebAgentCore } from '../common/browser-agent';
import type { WebPageAgentOpt } from '../web-element';
import type { BrowserContext as PlaywrightBrowserContext, Page as PlaywrightPage } from 'playwright';
import { WebPage as PlaywrightWebPage } from './page';
export type PlaywrightBrowserAgentOpt = Omit<WebPageAgentOpt, 'forceSameTabNavigation'> & {
    autoFollowNewPage?: boolean;
    newPageTimeout?: number;
};
export type PlaywrightBrowserAgentCreateOpt = PlaywrightBrowserAgentOpt & {
    initialPage?: PlaywrightPage;
};
export declare class PlaywrightBrowserAgent extends WebAgentCore<PlaywrightWebPage> {
    private readonly pageManager;
    constructor(context: PlaywrightBrowserContext, initialPage: PlaywrightPage, opts?: PlaywrightBrowserAgentOpt);
    static create(context: PlaywrightBrowserContext, opts?: PlaywrightBrowserAgentCreateOpt): Promise<PlaywrightBrowserAgent>;
    get activePage(): PlaywrightPage;
    pages(): PlaywrightPage[] | Promise<PlaywrightPage[]>;
    newPage(): Promise<PlaywrightPage>;
    setActivePage(page: PlaywrightPage): Promise<void>;
    waitForNewPage(action?: () => Promise<unknown> | unknown, opts?: {
        timeout?: number;
    }): Promise<PlaywrightPage>;
    destroy(): Promise<void>;
}
